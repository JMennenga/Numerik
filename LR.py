import numpy as np


np.matrix([[2,3,5],[6,12,22],[4,12,20]];


def LR(R):
    if np.size(R)[0]==np.size(R)[1]:
        L=np.eye(np.size(R))
        for i in range(np.size(R)[0]):
            
            for j in range(np.size(R)[1]):
